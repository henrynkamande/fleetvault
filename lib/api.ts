/**
 * Primary API client — use in services and React Query queryFns.
 * Default export matches fleetflow-frontend `import api from '@/lib/api'`.
 */
export { apiBaseUrl, default } from "@/lib/httpClient";
